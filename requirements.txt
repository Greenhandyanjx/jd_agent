# JD-Agent 依赖
# 核心
streamlit>=1.30.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# LLM
openai>=1.0.0                # OpenAI / DeepSeek / 兼容 API
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0   # 通义千问 ChatTongyi
dashscope>=1.20.0

# RAG / 向量存储
faiss-cpu>=1.7.0
langchain-text-splitters>=0.2.0

# Web
httpx>=0.27.0                # web_fetch 工具

# 文档处理
pypdf>=3.17.0

# 工具
PyYAML>=6.0
loguru>=0.7.0                # 结构化日志

# 测试
pytest>=7.0.0
pytest-asyncio>=0.23.0

# 部署
docker>=6.0.0

# PostgreSQL（可选，不装则回退 JSONL 文件存储）
asyncpg>=0.29.0

# Redis 缓存（四层缓存：LLM 响应/RAG/限流/Session）
redis[hiredis]>=5.0.0       # hiredis 是 C 实现的 Redis 协议解析器，比纯 Python 快 10 倍

# RabbitMQ 异步任务队列（非关键路径任务：Dream/Consolidation/Cleanup）
aio-pika>=9.0.0             # 异步 RabbitMQ 客户端（基于 asyncio + AMQP）
